apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentshop-api.fullname" . }}
  labels:
    {{- include "agentshop-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentshop-api.selectorLabels" . | nindent 4 }}
